#!/bin/bash
# commit-changes.sh is no longer needed as a standalone script.
# Validation and git operations are handled internally by chaos.ts.
# This wrapper exists for backward compatibility.
echo "commit-changes.sh is deprecated. Use chaos.ts directly." >&2
